"""Utilities to create dynamic SQLAlchemy models at runtime.

This is a placeholder that should be expanded when you need programmatic model generation.
"""


def create_dynamic_models(base, metadata):
    """Return dynamically generated models. Placeholder.

    Args:
        base: declarative base
        metadata: MetaData to bind
    """
    return {}
